import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        primary: {
          main: '#ab003c',
        },
        secondary: {
          main: '#eceef7',
        },
        background: {
          paper: '#fff',
          default: '#eceef1'
        },
      },
  });

export default theme